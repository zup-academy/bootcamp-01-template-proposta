spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
  h2:
    console:
      enabled: false
  jpa:
    properties:
      hibernate:
        show_sql: false
  profiles:
    active: test

cron:
  habilitada: false